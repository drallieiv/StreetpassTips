<div class="baitDetail">
  <div class="baitRow">
    {{#each baitDetail as |bait|}}
      <div class="baitCube" style={{dynamic-bg-color bait.color}} active="{{bait.active}}"></div>
    {{/each}}
  </div>
</div>
